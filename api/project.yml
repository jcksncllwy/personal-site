packages:
  - name: contact
    functions:
      - name: submit
        runtime: nodejs:18
        web: true
        limits:
          timeout: 15000
        environment:
          RESEND_API_KEY: ${RESEND_API_KEY}
